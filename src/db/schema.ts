// TODO: complete the schema
const schema = {
  tables: {
    product: {
      tableName: 'product',
      columns: {
        id: 'id',
        websiteId: 'website_id',
      },
    },
    website: {
      tableName: 'website',
    },
  },
};

export default schema;

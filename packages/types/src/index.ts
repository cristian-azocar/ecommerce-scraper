export interface IPagination {
  queryString: string;
}

export interface ISelectors {
  id: string;
  product: string;
  sku: string;
  name: string;
  platform: string;
  url: string;
  imageUrl: string;
  price: string;
  listPrice: string;
  discount: string;
  discountPercentage: string;
  availability: string;
  arrivalDate: string;
  condition: string;
  nextPage: string;
}
